using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Fungus;
public class TALKTOFUNGUS : MonoBehaviour
{
    string name = "rehaf";
    // Start is called before the first frame update
    void Start()
    {
        
    }

    public string getVarible() {
        return name;
     }

    // Update is called once per frame
    void Update()
    {
        
    }
}
